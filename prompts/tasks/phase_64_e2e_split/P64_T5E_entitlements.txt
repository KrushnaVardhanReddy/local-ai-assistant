MANDATORY RULES — VIOLATION = REJECTED PR:
1. NEVER comment out, stub, or mock existing implementation code.
2. All Playwright E2E tests MUST use `test.describe.serial` or run with `--workers=1` to prevent Wails binding collisions.
3. Every test MUST explicitly mock Wails window bindings using `page.evaluate()` or Playwright `route` handlers before interacting with the UI.
4. Commit message must start with "jules: " prefix.

Task: Expand E2E Test Suite - Entitlements & Billing
Overwrite the existing test file: `wails-app/frontend/tests/entitlement.spec.ts`

Instructions:
- Expose authState to Playwright via `window.__authState`.
- Write tests that inject `developer_mode = true` and verify the paywall is bypassed.
- Write tests injecting a `lifetime` plan verifying the "Buy License" button disappears.
- Write a test injecting a SaaS plan, incrementing token usage, and verifying the overage warning modal appears.
MANDATORY RULES — VIOLATION = REJECTED PR:
1. NEVER comment out, stub, or mock existing implementation code.
2. All Playwright E2E tests MUST use `test.describe.serial` or run with `--workers=1` to prevent Wails binding collisions.
3. Every test MUST explicitly mock Wails window bindings using `page.evaluate()` or Playwright `route` handlers before interacting with the UI.
4. Commit message must start with "jules: " prefix.

Task: Expand E2E Test Suite - RAG & Semantic Cache
Create a new test file: `wails-app/frontend/tests/cache.spec.ts`

Instructions:
- Write a test that clicks the Cache icon in the ActivityBar to open `CacheManagerModal.svelte`.
- Mock `window.go.backend.App.GetCacheStats` to return `{ hits: 15, sizeMB: 4.2 }`. Assert these numbers render in the DOM.
- Click the "Clear Cache" button and verify it successfully dispatches the clearing event by mocking `ClearState`.
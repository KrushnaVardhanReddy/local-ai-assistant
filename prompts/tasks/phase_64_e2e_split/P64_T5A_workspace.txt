MANDATORY RULES — VIOLATION = REJECTED PR:
1. NEVER comment out, stub, or mock existing implementation code.
2. All Playwright E2E tests MUST use `test.describe.serial` or run with `--workers=1` to prevent Wails binding collisions.
3. Every test MUST explicitly mock Wails window bindings using `page.evaluate()` or Playwright `route` handlers before interacting with the UI.
4. Commit message must start with "jules: " prefix.

Task: Expand E2E Test Suite - Core IDE & File System
Create a new test file: `wails-app/frontend/tests/workspace.spec.ts`

Instructions:
- Write a test targeting `CodeEditor.svelte` and `WorkspaceTabs.svelte`.
- Mock `window.go.backend.App.PromptOpenFile` to return a dummy file path and content.
- Assert that clicking a file in `FileTree.svelte` opens a new tab in `WorkspaceTabs.svelte` and the content renders in the `textarea`/editor.
TASK: P43-T1 — Local Vector DB in Go

═══════════════════════════════════════════════════════════════
BACKGROUND & CONTEXT
═══════════════════════════════════════════════════════════════

We are rebuilding our local AI backend entirely in Go. The first step is replacing the Python ChromaDB dependency with an embedded SQLite database that supports vector similarity search using the `sqlite-vec` extension.

═══════════════════════════════════════════════════════════════
OBJECTIVE
═══════════════════════════════════════════════════════════════

1. IMPLEMENT VECTOR DATABASE
   - Inside `wails-app/backend/`, create `vector_db.go`.
   - Use a Go SQLite driver (like `github.com/mattn/go-sqlite3`) that supports loading extensions, or specifically use `github.com/asg017/sqlite-vec-go-bindings`.
   - Implement a function to initialize a `qa_cache` table with a vector column for 384-dimensional embeddings.
   - Implement `InsertVector(id string, embedding []float32, metadata string)`.
   - Implement `SearchVector(query []float32, limit int)` to retrieve similar documents.

═══════════════════════════════════════════════════════════════
VERIFICATION
═══════════════════════════════════════════════════════════════

Commit message: "jules: P43-T1 implement embedded vector db in go"
Target branch: feature/krushna_golang

TASK: P57-T5 — Comprehensive BarnOwl IDE Button & Feature Audit, Wiring & Real-World Fixes

═══════════════════════════════════════════════════════════════
OBJECTIVE
═══════════════════════════════════════════════════════════════
Perform a complete end-to-end audit, repair, and test suite expansion for EVERY button,
drawer, mode toggle, and interaction across the BarnOwl AI Interview Workstation:
1. ActivityBar buttons (Folder, Ears, Brain, Mock, Keys, Settings)
2. Header Toolbar buttons (Snip, Report, Clear, Stealth Mode)
3. Workspace Drawer buttons & Tree ("FILE", "FOLDER", folder toggle, file tab select/close)
4. Brain Drawer buttons & actions ("STAR", "Catch Me Up", "Clear Cache", Markdown stream)
5. Live Ears Drawer buttons (Chat prompt submit, Pending chip send/dismiss, Clear All chips)
6. Backend Engine file parsing (broadening allowed extensions for code projects)

Previous Playwright tests passed with a shallow mock that bypassed real component event
wiring, masking critical real-world bugs. This task specifies the exact expected behavior
for EVERY button and feature, fixes all underlying issues, and backs them with exhaustive
real-world Playwright web interaction tests.

═══════════════════════════════════════════════════════════════
EXHAUSTIVE BUTTON & FEATURE SPECIFICATION
═══════════════════════════════════════════════════════════════

───────────────────────────────────────────────────────────────
GROUP A: ACTIVITY BAR (Leftmost 54px vertical bar)
───────────────────────────────────────────────────────────────
1. [Folder / Explorer] (`data-testid="activity-bar-explorer"`):
   - Role: Toggles the WorkspaceSidebar drawer open (`width: 240px`) or closed (`width: 0px`).
   - Bug: In `InterviewHUD.svelte`, `handleAction('explorer')` sets `activeAction = 'explorer'`,
     but does not update `IDEShell.svelte`'s internal `let isExplorerOpen` state.
   - Expected: Clicking toggles `isExplorerOpen`. Active state indicator lights up on button.

2. [Live Ears] (`data-testid="activity-bar-ears"`):
   - Role: Opens the right side-drawer displaying the real-time audio transcript history,
     live incoming speech chunk (`You`), and pending question suggestion chips.
   - Expected: Switches `activeDrawer = 'ears'`, mounts `<LiveEarsDrawer />`, highlights Ears icon.

3. [The Brain] (`data-testid="activity-bar-copilot"`):
   - Role: Opens the right side-drawer displaying live AI interview hints, code generation,
     and answer breakdowns.
   - Expected: Switches `activeDrawer = 'copilot'`, mounts `<BrainDrawer />`, highlights Brain icon.

4. [Mock Mode Toggle] (`data-testid="activity-bar-mock"`):
   - Role: Toggles simulated interview mode for practice.
   - Expected: When activated: starts mock voice/PTT loop, displays pulsing red "Mock" badge in
     top header. When deactivated: stops mock loop and automatically opens `<SessionReport />`.

5. [Hotkeys Panel] (`data-testid="activity-bar-keys"`):
   - Role: Toggles the keyboard shortcuts cheat-sheet panel.
   - Expected: Toggles `uiState.hotkeysPanelOpen = !uiState.hotkeysPanelOpen`. Renders `<HotkeysPanel />`.

6. [Settings] (`data-testid="activity-bar-settings"`):
   - Role: Opens the slide-over settings panel for Audio Device, Model, Prompt & Auth config.
   - Expected: Renders the settings drawer with `<Settings />`. Clicking close (x) returns to the
     previous drawer without breaking layout.

───────────────────────────────────────────────────────────────
GROUP B: HEADER TOOLBAR (Top horizontal bar)
───────────────────────────────────────────────────────────────
7. [Snip] (`button[title="Vision Snip"]`):
   - Role: Captures screenshot of technical interview problem/diagram and sends to vision LLM.
   - Expected: Calls `App.CaptureScreen()`, then calls `App.AnalyzeVision(b64, prompt)`.
     Switches drawer to Brain to stream the solution.

8. [Report] (`button[title="Session Report"]`):
   - Role: Displays session analytics, question turn count, elapsed time, and AI feedback scorecard.
   - Expected: Opens `<SessionReport onClose={() => showSessionReport = false} />`.
     Must not lock up the UI, must handle zero-turn/empty scorecard sessions gracefully,
     and must format duration correctly without `NaNm`.

9. [Clear] (`button[title="Clear Context"]`):
   - Role: Flushes conversational memory, transcript history, and active LLM context for next question.
   - Expected: Calls `App.ClearState()`, resets `wsState.transcript`, `wsState.response`,
     `wsState.ragSources`, `wsState.pendingTranscripts`, and `wsState.transcriptHistory`.

10. [Stealth Mode] (`button[title="Stealth Mode (Clickthrough)"]`):
    - Role: Toggles OS-level mouse clickthrough transparency.
    - Expected: Toggles `clickthrough = !clickthrough`, calls `App.SetClickthrough(clickthrough)`,
      highlights button with `.text-primary`, changes icon to mouse pointer.

───────────────────────────────────────────────────────────────
GROUP C: WORKSPACE SIDEBAR & TABS (Notes & Code IDE)
───────────────────────────────────────────────────────────────
11. [FILE Button] (`WorkspaceSidebar.svelte` -> `button[aria-label="Open File"]`):
    - Role: Prompts user to pick a local file (.md, .txt, .py, .go, .js, etc.).
    - Expected: Calls `App.PromptOpenFile()`. Upon selection, immediately refreshes IDE state,
      adds file to open tabs, activates it, and displays content in `<CodeEditor />`.

12. [FOLDER Button] (`WorkspaceSidebar.svelte` -> `button[aria-label="Open Folder"]`):
    - Role: Prompts user to select a workspace directory.
    - Expected: Calls `App.PromptOpenDirectory()`. Upon selection, immediately indexes all supported
      files, normalizes folder hierarchy, and renders tree in `FileTree.svelte`.

13. [Folder Expansion Chevron] (`FileTreeNode.svelte` -> `button.expander`):
    - Role: Expands or collapses a subfolder.
    - Bug: Go backend sends `isDir: bool`, but Svelte expects `isDirectory: bool`. Without
      normalization, folders fail to expand or show chevrons.
    - Expected: Clicking toggles folder children visibility and changes chevron `▶` / `▼`.

14. [File Tree Item Click] (`FileTreeNode.svelte` -> `.node-content`):
    - Role: Opens clicked file in the editor.
    - Expected: Calls `App.OpenFile(node.path)`, adds document to `openTabs`, switches active tab.

15. [Tab Select & Close] (`WorkspaceTabs.svelte`):
    - Role: Switch active open document (`onTabSelect`) or close document tab (`onTabClose`).
    - Expected: Clicking tab switches active content in `CodeEditor`. Clicking `✕` closes tab and
      selects adjacent tab or shows empty state if all tabs closed.

───────────────────────────────────────────────────────────────
GROUP D: THE BRAIN DRAWER (`BrainDrawer.svelte`)
───────────────────────────────────────────────────────────────
16. [STAR Method Quick Action] (`button.action-btn:has-text("STAR")`):
    - Role: Primes the LLM to format the next answer using Situation, Task, Action, Result.
    - Expected: Sends prompt override to assistant, sets `starPrimed = true`, shows pulsing
      `STAR Method Primed` indicator for 15s.

17. [Catch Me Up Action] (`button[title="Catch me up"]`):
    - Role: Requests a quick summary of the interview state.
    - Expected: Sends chat message "Please catch me up on the current context of the interview...".

18. [Clear Cache] (`button[title="Clear cache"]`):
    - Role: Flushes embedded semantic question/answer vector cache.
    - Expected: Calls `App.ClearCache()`, resets items count to 0.

───────────────────────────────────────────────────────────────
GROUP E: LIVE EARS DRAWER (`LiveEarsDrawer.svelte`)
───────────────────────────────────────────────────────────────
19. [Send Custom Prompt] (`textarea` + `Enter`):
    - Role: Submits an ad-hoc whisper/instruction to the AI without speaking.
    - Expected: Calls `sendChat(chatText)`, clears input, switches view or streams answer.

20. [Pending Suggestion Chip Click] (`.chip button.text-xs`):
    - Role: Candidate clicks an intercepted question chip to force instant answer generation.
    - Expected: Calls `sendChip(chip)`, dismisses chip, triggers LLM answer stream.

21. [Dismiss Chip / Clear All] (`.chip button[aria-label="Dismiss"]` / `Clear All`):
    - Role: Discards irrelevant suggestions.
    - Expected: Removes individual chip or flushes entire pending queue.

───────────────────────────────────────────────────────────────
GROUP F: BACKEND ENGINE EXTENSIONS (`wails-app/core/engine/workspace.go`)
───────────────────────────────────────────────────────────────
22. [Broaden Supported File Types]:
    - Role: Enable loading source code and interview cheat-sheets into workspace.
    - Bug: Engine restricts allowed files to `.txt`, `.md`, `.pdf`, `.pptx`. Loading folders with
      code files fails with "directory does not contain valid document files".
    - Expected: Add `.go`, `.py`, `.js`, `.ts`, `.jsx`, `.tsx`, `.svelte`, `.json`, `.rs`, `.cpp`,
      `.c`, `.h`, `.html`, `.css`, `.yaml`, `.yml`, `.sql`, `.sh` to `allowedExtensions`.

═══════════════════════════════════════════════════════════════
FILES TO MODIFY
═══════════════════════════════════════════════════════════════
MODIFY: wails-app/core/engine/workspace.go
MODIFY: wails-app/app.go
MODIFY: wails-app/frontend/src/products/interview/InterviewHUD.svelte
MODIFY: wails-app/frontend/src/lib/components/workspace/IDEShell.svelte
MODIFY: wails-app/frontend/src/lib/components/workspace/FileTreeNode.svelte
MODIFY: wails-app/frontend/src/lib/components/workspace/WorkspaceSidebar.svelte
MODIFY: wails-app/frontend/src/products/interview/BrainDrawer.svelte
MODIFY: wails-app/frontend/src/products/interview/LiveEarsDrawer.svelte
MODIFY: wails-app/frontend/tests/ui.spec.ts
MODIFY: prompts/tasks_v2.md

═══════════════════════════════════════════════════════════════
STEP-BY-STEP IMPLEMENTATION TASKS
═══════════════════════════════════════════════════════════════

1. Core Engine Extensions:
   - In `wails-app/core/engine/workspace.go`: expand `allowedExtensions` map with code/config extensions.
   - In `wails-app/app.go`: update `PromptOpenFile` file filters to allow all supported files.

2. State Synchronization & Normalization in `InterviewHUD.svelte`:
   - Bind `isExplorerOpen` state properly so clicking ActivityBar Folder toggles the sidebar.
   - Add `mapTree(nodes)` function:
     - Map `isDir` (Go backend) to `isDirectory` (Svelte).
     - Preserve user `isExpanded` folder state across polling refreshes.
   - Implement `refreshIDEState()` and call it immediately after:
     - `PromptOpenFile()`
     - `PromptOpenDirectory()`
     - `OpenFile()`
     - `SetActiveDocument()`
     - `CloseDocument()`
   - Fix `handleSelectNode` to check both `!node.isDirectory` and `!node.isDir`.

3. Defensive Attribute Access in `FileTreeNode.svelte`:
   - In `FileTreeNode.svelte`, compute:
     `const isDir = Boolean(node.isDirectory ?? (node as any).isDir);`
     so folder vs file detection is 100% immune to casing/naming differences.

4. Comprehensive Playwright Web E2E Suite (`wails-app/frontend/tests/ui.spec.ts`):
   Provide comprehensive interactive tests verifying real DOM mutations:
   - Test A: ActivityBar "Folder" toggles WorkspaceSidebar width between 0px and 240px (`.open` class).
   - Test B: Clicking "FILE" calls `PromptOpenFile`, renders new document tab in `WorkspaceTabs`.
   - Test C: Clicking "FOLDER" calls `PromptOpenDirectory`, renders file tree with folders and files.
   - Test D: Clicking a folder expander toggles child nodes visibility and updates chevron orientation.
   - Test E: Clicking a file in the tree opens it, switches active tab, and populates `CodeEditor`.
   - Test F: ActivityBar "Ears" and "Brain" switch right drawer between Live Ears and The Brain.
   - Test G: "STAR" quick action in Brain triggers `starPrimed` indicator.
   - Test H: "Snip" button invokes `CaptureScreen` and `AnalyzeVision`.
   - Test I: "Report" button opens SessionReport modal; close button cleanly hides it.
   - Test J: "Clear" button resets context and clears transcript and response.
   - Test K: "Stealth" button toggles clickthrough mode and applies `.text-primary`.

═══════════════════════════════════════════════════════════════
VERIFICATION
═══════════════════════════════════════════════════════════════
1. Run backend tests:
   `cd wails-app && C_INCLUDE_PATH=$PWD/backend/lib LIBRARY_PATH=$PWD/backend/lib CGO_CFLAGS="-I$PWD/backend/lib" CGO_LDFLAGS="-L$PWD/backend/lib -lwhisper -lstdc++ -lm" go test -v ./...`
   Must pass 100%.
2. Run Svelte checks:
   `cd wails-app/frontend && npm run check`
   Must report 0 errors.
3. Run Playwright E2E tests:
   `cd wails-app/frontend && npm run test:e2e`
   All interactive test suites must pass 100%.
4. Update `prompts/tasks_v2.md` with status and PR details.

TASK: P19-T3 — Session Token Enforcement

PARALLEL BATCH: 6 — Run after P19-T2.
Files touched: backend/auth.py, backend/app.py

PREREQUISITE: P19-T2 must be merged before this task.

═══════════════════════════════════════════════════════════════
OBJECTIVE
═══════════════════════════════════════════════════════════════
Enforce session limits for Pay-as-you-go users. 
They should be granted a JWT or tracked state with an expiration (e.g. `expires_at = now + 90min`). The backend must validate this continuously during active WebSocket connections and disconnect with a special message when the session expires.

═══════════════════════════════════════════════════════════════
CHANGES: backend/auth.py & backend/app.py
═══════════════════════════════════════════════════════════════

1. In `backend/auth.py`, implement logic to issue a 90-minute session token for `payg` users upon starting an interview.
2. In `backend/app.py`, update the main WebSocket event loop to continuously validate the session expiration.
3. If the token expires during a session, immediately send the following JSON payload to the frontend over the WebSocket:
   `{"type": "session_expired"}`
4. Close the WebSocket connection cleanly.

═══════════════════════════════════════════════════════════════
IMPLEMENTATION HINTS
═══════════════════════════════════════════════════════════════
- The check should happen on every incoming STT audio chunk or periodically in an asyncio background task to ensure prompt disconnection.

═══════════════════════════════════════════════════════════════
FILES LIST
═══════════════════════════════════════════════════════════════

FILES TO MODIFY:
- backend/auth.py
- backend/app.py

Commit: "jules: P19-T3 add 90-minute session enforcement for payg plan"
Target branch: main

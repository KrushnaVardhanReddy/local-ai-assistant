TASK: P42-T4 — Cloud Worker Proxy (Wails Edition)

═══════════════════════════════════════════════════════════════
BACKGROUND & CONTEXT
═══════════════════════════════════════════════════════════════

We must ensure that the Wails frontend correctly points to the Cloudflare Worker when the user is running the Cloud Edition of the app.

═══════════════════════════════════════════════════════════════
OBJECTIVE
═══════════════════════════════════════════════════════════════

1. CONFIGURE API ENDPOINTS
   - Open `wails-app/frontend/src/lib/api.ts` (or wherever the `getApiUrl()` and `getWsUrl()` logic lives).
   - Ensure that it reads `VITE_API_BASE` and correctly formats the HTTP and WS URLs to point to the Cloudflare Worker instead of the local Go backend.

═══════════════════════════════════════════════════════════════
VERIFICATION
═══════════════════════════════════════════════════════════════

Commit message: "jules: P42-T4 point Wails frontend to Cloudflare Worker"
Target branch: feature/krushna_golang

TASK: P56-T6 — CodeMirror 6 Editor & Clean Single-Window VS Code Layout

═══════════════════════════════════════════════════════════════
OBJECTIVE
═══════════════════════════════════════════════════════════════
Transform the UI into a clean, unified, single-window VS Code IDE layout.
Currently, the UI feels cluttered with 3 disjointed floating windows (the fixed 
sidebar drawer, the centered prompter box, and the floating copilot drawer at the bottom). 

We will unify everything into ONE cohesive, sleek window using `IDEShell.svelte` 
and embed CodeMirror 6 for real line numbers, syntax highlighting, and smooth speech auto-scrolling.

═══════════════════════════════════════════════════════════════
FILES TO CREATE & MODIFY
═══════════════════════════════════════════════════════════════
MODIFY: wails-app/frontend/package.json
CREATE: wails-app/frontend/src/lib/components/workspace/CodeEditor.svelte
MODIFY: wails-app/frontend/src/lib/components/workspace/IDEShell.svelte
MODIFY: wails-app/frontend/src/lib/components/workspace/WorkspaceSidebar.svelte
MODIFY: wails-app/frontend/src/lib/components/workspace/ActivityBar.svelte
MODIFY: wails-app/frontend/src/products/presenter/PresenterHUD.svelte
MODIFY: wails-app/frontend/tests/ui.spec.ts

═══════════════════════════════════════════════════════════════
DETAILED SPECIFICATION
═══════════════════════════════════════════════════════════════

1. Install CodeMirror 6 in `wails-app/frontend`:
   - In `package.json`, add:
     - "codemirror": "^6.0.1"
     - "@codemirror/lang-markdown": "^6.3.2"
     - "@codemirror/theme-one-dark": "^6.1.2"
     - "@codemirror/view": "^6.36.0"
     - "@codemirror/state": "^6.5.0"
   - Run `npm install` inside `wails-app/frontend`.

2. Reusable `CodeEditor.svelte`:
   Create in `wails-app/frontend/src/lib/components/workspace/CodeEditor.svelte`:
   - Props:
     - `content: string` (document text)
     - `readOnly?: boolean` (default true for presenter mode)
     - `fontSize?: number` (rem)
     - `lineHeight?: number`
     - `activeLine?: number` (line index driven by STT)
     - `onChange?: (text: string) => void`
   - Setup `EditorView` with:
     - `basicSetup` (line numbers, fold gutter, history)
     - `markdown()` language support
     - `oneDark` theme with transparent background override (`.cm-editor { background: transparent !important; }`)
   - Reactive Syncing:
     - When `content` changes, dispatch a transaction to replace doc if different.
     - When `activeLine` changes, smoothly scroll the line into center:
       `view.dispatch({ effects: EditorView.scrollIntoView(pos, { y: 'center' }) })`
       and add a subtle line highlight decoration (`.active-speech-line`).

3. Docking Layout in `WorkspaceSidebar.svelte` & `IDEShell.svelte`:
   - ELIMINATE `position: fixed` and floating overlay tabs!
   - In `WorkspaceSidebar.svelte`:
     - Change from `position: fixed` to a docked flex column that sits directly in the layout flow:
       `width: 240px; height: 100%; border-right: 1px solid rgba(255,255,255,0.08); background: rgba(18, 18, 18, 0.95);`
     - Remove the floating tab button sticking out (`.toggle-btn`); sidebar expansion is controlled via the ActivityBar Explorer icon or `Ctrl+B`.
   - In `IDEShell.svelte`:
     - Single unified window container:
       ```
       [ActivityBar (48px)] | [WorkspaceSidebar (240px)] | [Center Editor (flex-1)] | [Right Copilot Drawer (320px)]
                                                           ^ Top: WorkspaceTabs
                                                           ^ Center: <slot /> (CodeEditor)
       -------------------------------------------------------------------------------------------------------
       [Bottom StatusBar (24px)]
       ```
     - All 4 panels are physically docked inside the same flex layout.
     - When `isExplorerOpen` is false, the sidebar collapses (`width: 0; overflow: hidden;`).
     - When Copilot is open, it docks neatly on the right side (`width: 320px; border-left: 1px solid rgba(255,255,255,0.08)`). No floating windows!

4. Refactor `PresenterHUD.svelte`:
   - Replace the detached, floating layout with `<IDEShell>`:
     ```svelte
     <div class="presenter-container" style="background-color: rgba(10, 10, 10, {opacity});">
       <StealthTitleBar />
       <IDEShell
         workspaceTree={workspaceTree}
         openTabs={openTabs}
         activePath={activeDocumentPath}
         totalDocs={openTabs.length}
         status={ideStatus}
         onTabSelect={handleTabSelect}
         onTabClose={handleTabClose}
         onOpenFile={handleOpenFile}
         onOpenFolder={handleOpenFolder}
       >
         <CodeEditor
           content={script}
           fontSize={fontSize}
           lineHeight={lineHeight}
           activeLine={activeLineIndex}
           readOnly={true}
         />

         {#snippet rightDrawer()}
           <div class="copilot-panel">
             <div class="copilot-header">
               <span>Audience Copilot</span>
               <button on:click={() => isCopilotOpen = false}>✖</button>
             </div>
             <div class="copilot-body">
               {#if latestTranscript}
                 <div class="q">Q: {latestTranscript}</div>
               {/if}
               {#if latestResponse}
                 <div class="a">A: {latestResponse}</div>
               {/if}
             </div>
           </div>
         {/snippet}
       </IDEShell>
     </div>
     ```
   - Connect the speech tracking words to compute `activeLineIndex` and pass it to `CodeEditor`.
   - Move Typography sliders (Opacity, Font Size, Spacing) into a neat Settings popup triggered by the Activity Bar ⚙️ button.

5. Update Playwright Test in `tests/ui.spec.ts`:
   - In `tests/ui.spec.ts`, the test looks for `button[aria-label="Settings"]`.
   - Ensure the ActivityBar settings button has `aria-label="Settings"`.
   - When clicked, dispatch a custom event or toggle that opens the settings modal matching `div.settings-panel, .fixed.inset-y-0.right-0`.
   - Ensure `npm test` or `npx playwright test` passes 100%.

═══════════════════════════════════════════════════════════════
VERIFICATION
═══════════════════════════════════════════════════════════════
- `npm run check` and `npm run build` pass with 0 errors.
- `npx playwright test` passes with 0 failures.
- Run `make dev-presenter`:
  - Verify that the app is ONE single unified, cohesive window.
  - No detached floating boxes. The Activity Bar, Sidebar, Editor Pane, Copilot Panel, and Status Bar are all docked and seamlessly connected.
  - `Ctrl+B` toggles the docked sidebar.
  - Spoken words scroll the CodeMirror editor smoothly.

═══════════════════════════════════════════════════════════════
CONSTRAINTS
═══════════════════════════════════════════════════════════════
- Strictly adhere to Svelte 5 and TypeScript rules.
- Maintain glassmorphic transparency control (`--opacity`).
